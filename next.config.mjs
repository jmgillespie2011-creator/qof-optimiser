/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the floating Next.js dev-tools badge (the little box of options in the
  // corner during `next dev`). It never appears in production anyway.
  devIndicators: false,
};
export default nextConfig;
